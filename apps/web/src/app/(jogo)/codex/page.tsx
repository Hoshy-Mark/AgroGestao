import { EmConstrucao } from "@/components/EmConstrucao";

export default function CodexPage() {
  return (
    <EmConstrucao
      titulo="Codex"
      texto="Registro dos conceitos que você descobriu jogando (conversão alimentar, capital de giro, sazonalidade...) — GDD §13. Por enquanto só aparece um teaser na tela inicial."
    />
  );
}
