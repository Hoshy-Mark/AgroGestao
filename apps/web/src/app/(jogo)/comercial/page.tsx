import { EmConstrucao } from "@/components/EmConstrucao";

export default function ComercialPage() {
  return (
    <EmConstrucao
      titulo="Comercial"
      texto="Clientes, contratos de venda direta (Domain Bible §15) e consignação (§16) entram aqui. Hoje toda a produção ainda vira receita automaticamente, sem cliente nem negociação."
    />
  );
}
