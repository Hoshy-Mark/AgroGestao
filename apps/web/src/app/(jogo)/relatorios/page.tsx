import { EmConstrucao } from "@/components/EmConstrucao";

export default function RelatoriosPage() {
  return (
    <EmConstrucao
      titulo="Relatórios"
      texto="Histórico completo de produção por lote, custo por unidade e desempenho ao longo do tempo (GDD §21.4) entram aqui — a base (HistoricoProducao) já existe no banco, falta a tela."
    />
  );
}
